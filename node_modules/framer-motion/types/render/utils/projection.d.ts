import { ResolvedValues, VisualElement } from "../types";
import { LayoutState, TargetProjection } from "./state";
export declare function updateLayoutDeltas({ delta, layout, layoutCorrected, treeScale }: LayoutState, { target, relativeTarget }: TargetProjection, treePath: VisualElement[], transformOrigin: ResolvedValues): void;
